/* =========================
   BRAWLER IMAGE PATH
   Every brawler image lives at images/<name>.webp — generated from the
   key instead of being hardcoded per-brawler.
========================= */
function brawlerImg(name) {
    return `images/${name}.webp`;
}